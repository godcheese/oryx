<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gioov.nimrod.user.mapper.ViewMenuMapper">
    <resultMap id="BaseResultMap" type="com.gioov.nimrod.user.entity.ViewMenuEntity">
        <id column="id" property="id" jdbcType="BIGINT"/> <!-- id -->
        <result column="name" property="name" jdbcType="VARCHAR"/> <!-- 菜单名称 -->
        <result column="icon" property="icon" jdbcType="VARCHAR"/> <!-- 图标（icon） -->
        <result column="url" property="url" jdbcType="LONGVARCHAR"/> <!--请求地址（url）-->
        <result column="menu_category_id" property="menuCategoryId" jdbcType="BIGINT"/> <!-- 菜单分类 id -->
        <result column="sort" property="sort" jdbcType="BIGINT"/> <!-- 排序 -->
        <result column="remark" property="remark" jdbcType="VARCHAR"/> <!-- 备注 -->
        <result column="gmt_modified" property="gmtModified" jdbcType="TIMESTAMP"/> <!-- 更新时间 -->
        <result column="gmt_created" property="gmtCreated" jdbcType="TIMESTAMP"/> <!-- 创建时间 -->
    </resultMap>

    <sql id="TableName">
        `view_menu`
    </sql>

    <sql id="BaseColumnList">
        `id`, `name`, `icon`, `url`, `menu_category_id`, `sort`, `remark`, `gmt_modified`, `gmt_created`
    </sql>

    <insert id="insertOne" useGeneratedKeys="true" keyProperty="id"
            parameterType="com.gioov.nimrod.user.entity.ViewMenuEntity">
        insert into
        <include refid="TableName"/>
        (`id`, `name`, `icon`, `url`, `menu_category_id`, `sort`, `remark`, `gmt_modified`, `gmt_created`)
        values (#{id}, #{name}, #{icon}, #{url}, #{menuCategoryId}, #{sort}, #{remark},
        #{gmtModified}, #{gmtCreated})
    </insert>

    <update id="updateOne" keyProperty="id" parameterType="com.gioov.nimrod.user.entity.ViewMenuEntity">
        update
        <include refid="TableName"/>
        set `name` = #{name}, `icon` = #{icon}, `url` = #{url}, `menu_category_id` = #{menuCategoryId}, `sort` = #{sort}, `remark` =
        #{remark}, `gmt_modified` = #{gmtModified}
        where `id`= #{id}
    </update>

    <delete id="deleteOne" parameterType="long">
        delete from
        <include refid="TableName"/>
        where `id` = #{id}
    </delete>

    <delete id="deleteAll">
        delete from
        <include refid="TableName"/>
        where `id` in
        (
        <foreach collection="list" item="item" index="index" separator=",">
            #{item}
        </foreach>
        )
    </delete>

    <select id="getOne" resultMap="BaseResultMap">
        select
        <include refid="BaseColumnList"/>
        from
        <include refid="TableName"/>
        where `id` = #{id} limit 1
    </select>

    <select id="pageAll" resultMap="BaseResultMap" parameterType="com.gioov.common.mybatis.Pageable">
        select
        <include refid="BaseColumnList"/>
        from
        <include refid="TableName"/>
        ${pageable}
    </select>

    <select id="listAllByMenuCategoryIdAndRoleId" resultMap="BaseResultMap">
        select
        <include refid="BaseColumnList"/>
        from
        <include refid="TableName"/>
        where `menu_category_id` = #{menuCategoryId}
    </select>

    <select id="pageAllByMenuCategoryIdAndRoleId" resultMap="BaseResultMap">
        select
        <include refid="BaseColumnList"/>
        from
        <include refid="TableName"/>
        where `menu_category_id` = #{menuCategoryId} ${pageable}
    </select>
    <select id="countAllByMenuCategoryIdAndRoleId" resultType="int">
        select count(1) from
        <include refid="TableName"/>
        where `menu_category_id` = #{menuCategoryId}
    </select>

    <select id="getOneByMenuCategoryId" resultMap="BaseResultMap">
        select
        <include refid="BaseColumnList"/>
        from
        <include refid="TableName"/>
        where `menu_category_id` = #{menuCategoryId} limit 1
    </select>

    <select id="searchAllByName" resultMap="BaseResultMap">
        select
        <include refid="BaseColumnList"/>
        from
        <include refid="TableName"/>
        where `name` like '%${name}%'
    </select>

    <select id="listAllByMenuCategoryIdAndRoleIdList" resultMap="BaseResultMap">
        select
        <include refid="BaseColumnList"/>
        from
        <include refid="TableName"/>
        where `menu_category_id` = #{menuCategoryId} and `id` in (
        select `view_menu_id` from `role_view_menu` where `role_id` in (
        <foreach collection="roleIdList" item="item" index="index" separator=",">
            #{item}
        </foreach>
        )
        )
    </select>
    <select id="listAllByMenuCategoryIdList" resultType="com.gioov.nimrod.user.entity.ViewMenuEntity">
        select
        <include refid="BaseColumnList"/>
        from
        <include refid="TableName"/>
        where `menu_category_id` = #{menuCategoryId}
    </select>

    <select id="pageAllByMenuCategoryIdList" resultMap="BaseResultMap" parameterType="com.github.pagehelper.Page">
        select
        <include refid="BaseColumnList"/>
        from
        <include refid="TableName"/>
        where `menu_category_id` in (
        <foreach collection="menuCategoryIdList" item="item" index="index" separator=",">
            #{item}
        </foreach>
        )
    </select>

</mapper>
